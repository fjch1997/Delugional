namespace Delugional
{
    public enum TorrentState
    {
        Error,
        Moving,
        Paused,
        Queued,
        Checking,
        Downloading,
        Seeding,
        Allocating
    }
}
